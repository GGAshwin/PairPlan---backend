meta {
  name: Join workspace
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/workspace/join
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "join_code": "QDRH"
  }
}
